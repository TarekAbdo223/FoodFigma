import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MeditationCards = () => {
  return (
    <View>
      <Text>MeditationCards</Text>
    </View>
  );
};

export default MeditationCards;

const styles = StyleSheet.create({});
